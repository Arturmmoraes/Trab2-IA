A rede neural foi implementada no agents.py

A Meta Heurística do Cardume de Peixes foi implementada no fish_algo.py

Para treinar e testar basta rodar train_fss_agent.py (irá treinar, testar e criar tabelas e gráficos)

Para apenas testar com o best_weights.npy e fss_history atuais basta rodar test_trained_agent.py (irá testar e criar as tabelas e gráficos)

É importante dizer que o fss_history não se apaga sozinho, portanto, é necessário remover as iterações salvas anteriormente no arquivo caso faça o treinamento novamente.